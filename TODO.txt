### TODO ###

-Neuer Auftrag
  -Default end_of_timespan ist aktuelle Zeit+duration von Programm
  -Bestaetigung

-Auftragsliste:
  -finished=0
  -Nur aktive Auftraege
  -Zeit vernuenftig
  -Progressbar

-HAML

-tooltips

-Devise
  -Developer Bar

-ActiveAdmin

-Background Tasks

-Nutzer sieht alle Auftraege, editieren, loeschen

-Foreignkeys mit Loesch-Abhaengigkeiten

-Date-Time-Picker richtig

-Mehr Validations

-YAML-Import

-Action-Mailer
  -Exception-Mailer?

-Konfliktmanagement
  -Feld in Job: start:datetime
  -Wenn kein Auftrag für Device:
    ->Wenn 12:00Uhr zwischen aktuelle Zeit und :end_of_timespan
      ->start=12:00Uhr
    ->Wenn 12:00uhr nicht zwischen aktueller Zeit und :end_of_timespan
      ->start=aktuelle Zeit

  -Wenn Auftrag im Device:
    ->Wenn start+duration(Auftrag1) < end_of_timespan(Auftrag2):
      ->start(Auftrag2) = start+duration(Auftrag1)
    
    ->Wenn start(a1)+duration(a1)+duration(a2) < end_of_timespan:
      ->start(a1)= unveraendert
      ->start(a2)= start(a1)+duration(a2)
    ->Wenn start(a1)+duration(a1)+duration(a2) > end_of_timespan &&
      start > aktuelle Zeit:
      ->Wenn aktuelle Zeit+duration(a1)+duration(a2) < end_of_timespan:
        ->start(a1)=aktuelle Zeit
        ->start(a2)=aktuelle Zeit+duration(a1)
      ->sonst
        -> Wird nicht mehr rechtzeitig fertig
    ->sonst
        -> Wird nicht mehr rechtzeitig fertig
  